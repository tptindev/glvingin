#ifndef ENTITYIDS_H
#define ENTITYIDS_H

class EntityIDs
{
public:
    enum MenuScene
    {
        BACKGROUND,
        START_BUTTON,
        SETTING_BUTTON,
        EXIT_BUTTON,
    };

    enum GameScene
    {
        PLAYER
    };
};

#endif // ENTITYIDS_H
